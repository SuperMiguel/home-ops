# Secrets
*.pub
*.key
*.decrypted~*.yaml
.decrypted~*
/age.key
/cloudflare-tunnel.json
/github-deploy.key
/github-deploy.key.pub
/github-push-token.txt
# Template config files
/cluster.yaml
/nodes.yaml
# Kubernetes
kubeconfig
talosconfig
# Helm (local helm pull / template — charts come from OCI in Argo)
*.tgz
# Misc.
.private/
.task/
.venv/
.DS_Store
Thumbs.db
